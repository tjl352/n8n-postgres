# Data backups (store in Dropbox / Drive, not Git)
*.sql
*.tar.gz
backups/
n8n-backups/

# Local env / secrets
.env
.env.*
!.env.example

# Optional local MCP overrides (shared templates stay in git)
.cursor/mcp.json.local
.mcp.json.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
